import { ComposantInterceptor } from './composant.interceptor';

describe('ComposantInterceptor', () => {
  it('should be defined', () => {
    expect(new ComposantInterceptor()).toBeDefined();
  });
});
